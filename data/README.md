# Synthetic Data

This public repository contains synthetic healthcare operational data only. The large generated event file is intentionally not committed; use `scripts/generate_synthetic_data.py --rows 20000 --seed 42` to reproduce it locally. No real patient data are included.
